# OnSig Mobile — copy to .env.local for local dev.
#
# Variables prefixed with EXPO_PUBLIC_* are inlined at build time and
# visible in the bundle — never put secrets here.

EXPO_PUBLIC_API_URL=https://onsig-prod.fly.dev/api
